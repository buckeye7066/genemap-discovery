name: Release language policy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Exercise adversarial scanner probes
        run: python3 scripts/check-release-language.py --self-test
      - name: Scan Git-tracked text
        run: python3 scripts/check-release-language.py
