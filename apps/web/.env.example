VITE_API_URL=http://localhost:3000

# Optional browser error tracking (baked at BUILD time — set in Vercel and
# redeploy to activate). Unset = no-op; errors still reach the backend
# owner-email pipeline.
# VITE_SENTRY_DSN="https://<key>@<org>.ingest.sentry.io/<project>"
